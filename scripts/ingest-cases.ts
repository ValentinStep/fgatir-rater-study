/**
 * ingest-cases.ts
 *
 * Ingestion script that:
 * 1. Reads DICOM files from original and denoised directories
 * 2. Strips PHI (patient identifying information)
 * 3. Removes SeriesDescription (reveals condition)
 * 4. Generates anonymized file names
 * 5. Creates a manifest JSON for the study
 *
 * Usage: npm run ingest-cases
 *
 * This script will be implemented in a future milestone.
 */

console.log('ingest-cases: Not yet implemented');
console.log('Will process DICOM files and create study manifest.');
process.exit(0);
