// Copy this file to `config.js` and fill in your own Supabase project values.
// `config.js` is gitignored so your keys never get committed.
//
// The SUPABASE_KEY here should be your project's PUBLISHABLE / anon key
// (safe for client-side use, protected by Row Level Security policies).
// NEVER put a service_role / secret key in a browser app.
window.APP_CONFIG = {
  SUPABASE_URL: "https://YOUR_PROJECT_REF.supabase.co",
  SUPABASE_KEY: "YOUR_SUPABASE_PUBLISHABLE_OR_ANON_KEY",
  STORAGE_BUCKET: "images"
};
