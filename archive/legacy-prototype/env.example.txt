# Copy this file to `.env` and fill in your own values.
# These are the same values used by config.js (the browser app reads them
# from config.js at runtime; this .env is provided for tooling / scripts /
# your own deployment pipeline). Never commit the filled-in .env.

# Your Supabase project URL, e.g. https://abcdefgh.supabase.co
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Your Supabase PUBLISHABLE / anon key (client-safe, RLS-protected).
# Do NOT use a service_role / secret key in a browser context.
SUPABASE_KEY=YOUR_SUPABASE_PUBLISHABLE_OR_ANON_KEY

# Public storage bucket that holds manifest.json and the image slice folders.
STORAGE_BUCKET=images
